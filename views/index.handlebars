<h1>Eat Da Burger!</h1>
<div><img src="/assets/img/burger-solid.png" alt="Da Burger" width="42" height="42"></div>

<h2>Burgers not eaten!</h2>

{{!--
<div>{{burger}}</div> --}}

<ul>
  {{#each burger}}
  
    {{#unless devoured}}<li>{{burger_name}}
    
    {{!-- <button class="change-sleep" data-id="{{id}}" data-newsleep="{{sleep}}">
      {{#if sleep}}SLEEP TIME!{{else}}WAKE UP!{{/if}}
    </button> --}}

    <button class="devourtheburger" data-id="{{this.id}}">DEVOUR!</button>
    </li> {{/unless}}

  {{/each}}
</ul>

{{!--
<ul>
  {{#each burger}} {{#unless devoured}} {{> cats/cat-block sleep=true}} {{/unless}} {{/each}}
</ul> --}}

<h2>Burgers eaten!</h2>

<ul>
  {{#each burger}} {{#if devoured}}
  <li>{{burger_name}}</li> {{/if}} {{/each}}
</ul>

{{!--
<ul>
  {{#each cats}} {{#if sleepy}} {{> cats/cat-block sleep=false}} {{/if}} {{/each}}
</ul> --}}

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="newBurg" name="name">
  </div>

  <button type="submit" class="addburger">Add Burger</button>
</form>

<script src="/assets/js/frontend.js"></script>